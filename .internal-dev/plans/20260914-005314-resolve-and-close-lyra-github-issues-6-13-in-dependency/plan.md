# Resolve and close Lyra GitHub issues #6–#13 in dependency-aware edit tranches

## Feature

Implement every GitHub issue OPEN for dhickel/lyra at the authorized planning snapshot: #6 through #13. Group changes by shared edit surfaces into complete, validated units: primitive initialization; an end-to-end source/command migration; retained nominal member contracts and route authority; call homogeneity and performance; final qualification and issue closeout. Preserve the complete issue/comment acceptance matrix, existing regression coverage, exact typed JVM execution and runtime authority boundaries. Close issues only with committed implementation and passing validation evidence.

## Required Behavior

- Scope and evidence: re-query GitHub at execution start and capture bodies and all comments for #6–#13, mapping every acceptance item and subsequent owner clarification to a target, tranche, test and eventual commit. These eight snapshot issues are the implementation scope; closed #2–#5 receive regression validation only. Reconcile material requirement or issue-state drift explicitly rather than silently expanding or shrinking scope. A newly opened unrelated issue is not automatically added.
- #13: remove all primitive alias fields from compiler and runtime LyraType, migrate all repository Java consumers, static imports, tests and API examples to PrimitiveType, and document the intentional Java source/binary compatibility break. In each module use genuinely isolated loaders that initialize LyraType first and PrimitiveType first; verify all fourteen enum constants and their type operations are non-null/correct, and assert the removed alias fields are absent. Do not require reproducing a compiler failure that is not currently deterministic; distinguish the compiler's abstract interface methods from the runtime's default-method initialization hazard.
- #9: retain parenthesized (match ...), (iter ...) and (while ...); accept only bare match[...], iter[...] and while[...] as their bracket spellings. Reject obsolete ::match[...], ::iter[...] and ::while[...] with stable source-mapped parser diagnostics, including qualified/direct-accessor variants that attempt to use reserved special forms as callable names. Preserve loop callback selection, types, effect order, termination and cancellation semantics. Ordinary resolved callable names and methods still use ::.
- #10: accept lexically adjacent bare negative numeric spellings, including signed minima, suffix and exponent variants and negative floating zero, in expression positions. Keep nonnegative literal magnitudes and normalize through the existing unary-minus semantics/IR. Preserve contextual and forced typing, suffix/range checks, floating underflow/nonfinite checks, overflow and unsigned rejection. Minus separated from the number by whitespace, comments or newline is not a bare negative literal; existing (- 1) and -[1] operator forms remain valid. Preserve existing operator-head/arity rules and exponent signs; do not accidentally turn binary subtraction into signed-minimum permission. Add malformed -, --1, invalid suffix, range-step and index-boundary diagnostics.
- #11: reserve cond; require a real subject for value match; remove ?? arms and reject obsolete conditional (match _ ...), including bracket equivalents. Both (match ...) and match[...] use marker-free arms and preserve subject-once evaluation, source-ordered lazy expression patterns, optional when guards, typed equality/lossless widening/structural equality, and mandatory final unguarded wildcard. Parenthesized (cond condition -> result ... _ -> fallback) alone provides subjectless ordered truthiness tests, no when, mandatory fallback and the former conditional-match result unification/nilability behavior. A fallback-only match evaluates its subject; fallback-only cond evaluates only its fallback. Preserve flow, provenance, effects, source maps and result tail positions; do not add match-specific general narrowing.
- Implement delimiter-, nesting- and context-aware match/cond arm parsing rather than token splitting. Cover namespace and arm arrows, postfix chains, nested match/cond, comments, explicit nominal construction, bare built-ins, direct-call expressions and bounded malformed-input recovery with exact UTF-16 spans. Keep a dedicated source Cond representation and distinguish its source identity in descriptors, AST, typed metadata, diagnostics and source maps; use the smallest adequate closed IR representation and update all affected inventories and validators.
- #12: nominal construction uses only :Type[args...] or :module->Type[args...]. Reject resolved nominal use of old unprefixed construction, including the old qualified namespace-value-access form, with a stable migration diagnostic. Resolution, not capitalization, must distinguish old nominal applications from ordinary value indexing; uppercase value identifiers remain indexable. Preserve unprefixed primitive/String conversions and Array/Tuple construction, including typed, empty and Unit forms. Preserve declarations, exact type lookup/visibility, arity, left-to-right arguments, field/default/constructor-body order, nominal identity, factory capability and generated descriptor semantics, flow certification and structured failures. New source construction lowers to existing construction/factory semantics unless a necessary closed-IR change is demonstrated. No runtime speedup or byte-for-byte artifact identity is claimed.
- Replace developer commands with one leading backslash: \help, \bindings, \type, \load, \reload, \reset, \history and \quit. Remove colon aliases. Every colon-leading input unit is Lyra source; classification, completeness checking, opening files and editor analysis never execute it, while explicit submission executes normally. Keep \type non-executing, unknown commands structured, arguments bounded and source arguments intact. Align plain, JLine, attach and editor dispatch, completion, highlighting, multiline handling, help and source-only history. Recognize commands only at the existing top-level command boundary, not inside pending source, strings/comments or program readLine input; preserve protocol v2 operation schemas.
- #6 named-call model: after resolution, (f args) and ::f[args] share canonical call semantics/lowering whenever the same exact declaration/storage route is proven and its entry/write boundaries authenticate the complete callable contract. Cover local/recursive functions, parameters, captures, mutable cells, imports, selective/qualified imports, session-linked names and intrinsics. Shape, descriptor or spelling alone is not proof. If an existing direct path has an insufficient boundary, repair that boundary for both spellings rather than copying an unsafe bypass. Aggregate/index projections, opaque returned callables and other unproven expression-derived targets remain dynamic with full authentication.
- #6 method/namespace model: preserve receiver::method[args] and namespace->class::multi2[10 20]; S-expression method invocation reads the callable value with :. and invokes it, as in (receiver:.method args) or (namespace->class:.multi2 10 20). For a proven route both forms evaluate the receiver and select the current slot once before arguments, preserve selected receiver/callable semantics, and have equivalent behavior and lowering. Argument effects that replace the slot must not change the already-selected target. A saved member reference keeps its original selection.
- #6 grammar boundary: retain receiver postfix value::method[...] without a comma. Require/allow a comma only to disambiguate a subsequent sibling expression starting with ::, extending this boundary to program/block sequences as well as existing lists. Do not require commas between ordinary forms or add general comma punctuation to blocks. Apply the narrow exception consistently where marker-free match/cond expressions would otherwise absorb a sibling :: head; ordinary comma-separated arms remain invalid. Separators are unnecessary immediately after an arm arrow or opening delimiter. Reject leading, doubled, trailing and otherwise misplaced commas in newly enabled contexts. Special-case (::f[args]) as preservation of the direct call, not a zero-argument application of its result; do not add general grouping or semicolons.
- #6 self-tail and failure behavior: give proven S-expression self-recursion the same constant-stack lowering as F-expression self-recursion, including match/cond result positions. Exact storage-route proof is not by itself proof that a mutable slot still contains the currently executing closure: preserve existing rebinding/alias safeguards and fall back to normal invocation when self identity is not established. Translate StackOverflowError crossing generated call boundaries, including target selection/authentication, argument evaluation and invocation, to source-mapped LYR-STACK. Include intrinsics and generated adapters where they form those boundaries. Preserve nested frame ordering without duplicate frames, and let all other VirtualMachineError instances escape unchanged.
- #6 dynamic path: plan deterministic per-generated-state/closure-instance fields for each distinct exact expected callable signature used by that instance. Initialize after producer authority and exact nominal schema availability, before use, without reading uninitialized source state or executing source. Reuse those fields rather than per-call parsing or producer-cache lookup; never add a global unscoped signature/nominal cache. Dynamic invocation still authenticates the candidate, complete signature, applicable route evidence, owner/artifact/session, lifecycle, thread and safe point on every call. Metadata cannot authorize live values. Include initialization failure and multiple independent instance tests.
- #6 associated defects: fix nilable-Fn coalesce interface class planning/checkcast ordering so valid source compiles/verifies/executes and invalid source yields an ordinary diagnostic, never LyraCompilerBugException. Cover the sibling and parenthesized direct-call cases, nominal method parity on the completed nominal backend, and the full S/F homogeneity matrix. Preserve intentional syntax asymmetry for genuinely computed targets lacking a direct-name spelling.
- #6 performance: compare normalized call instruction/control-flow shapes for proven equivalent S/F calls, excluding source-dependent labels, offsets, identities and constant-pool numbering from equality. Require no redundant per-call LyraSignature.parse, producer-signature-cache lookup or requireAuthenticatedForGeneratedInvocation block on the proven path; required storage/member-entry checks and callee lifecycle/safe-point checks remain. Add paired fib/direct-name JMH rows under the complete Phase 23 average-time nanoseconds protocol: one thread, at least two forks, three one-second warmups, five one-second measurements and GC profiler. Use JMH primaryMetric.score(S)/primaryMetric.score(F) <= 1.10, not whichever median/score is more favorable. Inputs, results and harness boundaries must be equivalent, with compilation/loading outside steady-state measurement and observable outputs preventing folding. Keep all existing gates and mandatory structural checks.
- #8: derive nominal member read contracts independently at typed semantic provenance/flow sealing and IR validation from the exact resolved schema/member, including nilability. Same- and cross-generation explicit annotations, coalesce, predicate narrowing and value match over nilable reads must compile and execute under current nil rules. Continue rejecting nilable receivers, mismatches, forged member links and invented match narrowing; valid cases must not reach LYC-IR-003. Preserve retained transfer certification rather than relaxing it.
- #7: first authenticate the exact retained nominal/object, schema, producer and compiler-certified field route, then delegate narrowly to callable/nominal leaves selected through that route. Do not change SessionStorageDomain.Linkage.sourceLocal or general same-session/importing-graph authentication. Preserve producer-bound factories, initialized storage, object/closure identity, owner thread, lifecycle/reset/close/root dependencies, complete signatures/schemas, private access and failure ordering. The field-derived callable remains usable as an ordinary saved value through assignment, capture, aggregate storage, parameter/return propagation and later same-session submissions with exact carried route/provenance evidence, not copied/replayed state.
- #7 authority precision: scope evidence to the authenticated value occurrence and exact selected field route/value, not merely closure identity, class, shape or session membership. A successful delegated read must not globally bless the same raw imported closure. Reject raw imports both before and after authorized delegated use, including when routed and raw occurrences coexist in a tuple/array, capture or later submission. A saved callable remains its selected value after the field is replaced; a new field read selects/authenticates the replacement. Do not re-read mutable fields at call time or invalidate saved selection solely because the current slot differs. Neither saved evidence nor metadata permits use after its certified lifecycle/epoch/root dependencies retire. Use bounded compiler/session evidence integrated with existing exact typed storage; avoid general heap scans or closure-wide authorization tables.
- #7 pinned inventory: flip all four retained nominal Unit/intrinsic cases together: namespace member callable, namespace direct-call Unit, iter Unit and while Unit. Assert object observation and member values, actual callable invocation where applicable, and exactly-once effects across construction and later generations. Add wrong/forged routes, fields, schema/type/signature, private member, unrelated session/root, initializing/failed/closed producer, reset, direct imported closure, nested aggregate/nominal/callable, saved/captured/returned/rebound and later-generation cases. Keep existing SessionClosureAuthorityTest assertions intact.
- In each production change update the corresponding positive, negative, boundary, runtime-failure, conformance, inventory, generator, independent oracle/model and developer coverage surfaces. Migrate active valid fixtures/examples to accepted syntax. Preserve original saved counterexample bytes/transcripts as historical or explicit old-syntax rejection evidence, and add migrated semantic equivalents so the original bug is still exercised; do not overwrite a saved reproduction or let a new syntax error replace its semantic regression. Expected results remain independent of production helpers.
- Update living language/backend/repl/editor specifications and decisions to supersede signs, ??/match _, :: built-ins, bare nominal construction, colon commands, sibling-call limits and direct-only tail lowering. Update grammar, README, docs/examples, release matrices, knowledge and bug records in the same completed units. Do not mechanically rewrite historical records as if they described the new language.
- Version the source-breaking language contract coherently across compiler and runtime revision/export identity derivation, emitted/embedded metadata and loader checks. Update hard-coded metadata publication assumptions as well as constants. Audit removal of runtime alias fields and generated route/signature helper changes against RuntimeAbi compatibility; increment the applicable ABI version when required and explicitly test supported/rejected artifact combinations. Preserve schema-version meaning separately from language/runtime ABI versioning. No silent compatibility adaptation, edition selector or second execution ABI is introduced.
- Every completed tranche includes its consumers, tests/models/docs/specification impact, focused validation, full mvn test, diff review, changelog and commit before the next tranche. Run extended fuzz for language/runtime changes. Internal substeps that cannot independently meet these requirements are not completed phases. Preserve unrelated work; do not push. GitHub evidence comments and closure occur only after all criteria for the issue and its grouped validation pass; leave/reopen issues when a criterion is unmet.

## Targets

- `GitHub dhickel/lyra issues #6–#13; .internal-dev/bugs/callable-call-per-call-authentication-overhead/report.md; .internal-dev/bugs/retained-nominal/*.md; .internal-dev/bugs/compiler/lyratype-primitive-init-cycle.md`: Capture paginated issue/comment acceptance evidence, preserve reproductions, reconcile related closed records, and record exact tests/commits before closure. Archive only resolved/superseded local reports under the workflow contract.
- `.internal-dev/specifications/{language-core,backend-runtime,repl,editor,decisions}.md`: Record accepted breaking syntax/command decisions, exact call/tail/route boundaries, versioning and qualification contracts in their existing owning specifications.
- `lyra-compiler/src/main/resources/grammar_spec.md; README.md; docs/{repl,editor,language-testing}.md; active source examples and relevant release/API documentation`: Synchronize new spellings, contextual separators, negative literals, marker-free match/cond, explicit nominal construction, backslash commands, Java primitive API migration and maintained coverage, while preserving historical records.
- `lyra-compiler/src/main/java/io/mindspice/lyra/compiler/{lex,grammar,parse,ast}/** and compiler diagnostic definitions`: Coordinate token/adjacency handling, productions, descriptors/replay/visitors, source Cond identity, explicit construction, sibling/direct-call boundaries and structured obsolete/malformed-syntax diagnostics with exact spans.
- `lyra-compiler/src/main/java/io/mindspice/lyra/compiler/semantic/**, including TypedSemanticProvenance, topology validation, callable summaries and semantic/flow/**`: Resolve/type new syntax; establish canonical exact named-call proof; preserve effects, refinements, self identity and retained flow; independently derive nominal member contracts and route evidence.
- `lyra-compiler/src/main/java/io/mindspice/lyra/compiler/ir/** and api/SessionFlowCertificate.java plus related session certificate/retained transfer API types`: Maintain the closed IR and exhaustive validators, minimal Cond/call representation, independent member/route proof sealing, transfer/summary provenance and exact saved-value evidence across generations.
- `lyra-compiler/src/main/java/io/mindspice/lyra/compiler/backend/jvm/{JvmBytecodeEmitter,GeneratedTypePlanner}.java and adjacent generated class/member/nominal layout planning classes`: Emit unified proven S/F calls, self-tail safeguards, complete LYR-STACK regions, per-instance signature fields, correctly preplanned nilable function interfaces, lazy new syntax semantics and certified nominal field delegation.
- `lyra-compiler/src/main/java/io/mindspice/lyra/compiler/types/{LyraType,PrimitiveType}.java; lyra-runtime/src/main/java/io/mindspice/lyra/runtime/{LyraType,PrimitiveType}.java; all repository Java consumers/static imports`: Remove aliases in both modules, migrate enum-owned constants and document/test both initialization orders and the public API break.
- `lyra-runtime/src/main/java/io/mindspice/lyra/runtime/{LyraClosure*,LyraNominal*,LyraOwnershipToken,LyraArtifactKey,SessionStorageDomain,NominalSchema*}.java and adjacent exact type/authority support`: Integrate narrow occurrence/route delegation and producer-scoped signature initialization without widening sourceLocal, general authentication, private access or lifecycle authority.
- `lyra-runtime/src/main/java/io/mindspice/lyra/runtime/{LyraRuntimeConstants,RuntimeAbi,ModuleRevision,ArtifactRevision,ExportId,ArtifactMetadata,ArtifactMetadataReader,RuntimeOptions}.java; lyra-compiler/src/main/java/io/mindspice/lyra/compiler/source/{ModuleRevision,ModuleGraph}.java; compiler/artifact/ArtifactAssembly.java; backend/jvm/JvmExportId.java and associated metadata/version fixtures`: Synchronize language revision/version identity and runtime compatibility; remove literal language-version assumptions in embedded facade metadata publication; prove deterministic current-version packaging and explicit incompatible-artifact rejection.
- `lyra-repl/src/main/java/**, including ConsoleCommandParser, PlainConsole and session publication/storage integration; lyra-cli/src/main/java/**; lyra-editor/src/main/java/**`: Migrate dispatch/completeness/help/completion/highlighting/history to backslash commands and new syntax, preserve protocol operations/nonexecuting analysis and carry exact route evidence through initialized session storage.
- `Compiler tests and fixtures: LexerTest, GrammarMatcherTest, ParserTest, NominalSyntaxTest, NominalSemanticsTest, TypeCheckerTest, TypedIrTest, MatchBoundaryIntegrationTest, CallbackLoopIntegrationTest, NominalBytecodeTest, Phase23StructuralBytecodeTest, LanguageCoverageTest, MatchFuzzRegressionTest, corpus/replays, TypedProgramGenerator, LanguageFuzzWorker, RetainedNominalModel and related suites`: Update all affected syntax, semantic, evaluation-order, flow, type, bytecode, diagnostic, source-map, tail, numeric and independent fuzz/model coverage with explicit migrated and obsolete-form cases.
- `Runtime/REPL/CLI/editor tests including NominalTypeContractTest, NominalConstructionTest, NominalArtifactMetadataTest, SessionClosureAuthorityTest, NominalSessionTest, MatchSessionTest, console/JLine/cross-surface tests, LanguageServiceTest, EditorRuntimeTest and EditorWindowTest`: Add isolated initialization, retained member nilability, occurrence-specific route anti-laundering, selected-value/rebinding/lifetime, actual invocation/effects, command/source classification, attachment and graphical editor regressions.
- `lyra-compiler/src/jmh/java/io/mindspice/lyra/compiler/benchmark/Phase23Benchmark.java; tools/phase23-evidence.sh; tools/phase23-gates.json; tools/phase23_gate.py; Phase23EvidenceGateContractTest and structural tests`: Add equivalent S/F direct-name/fib score rows, enforce <=1.10 average-time score ratio within the full existing protocol, extend evidence normalization/reporting and evaluator self-tests for pass/fail/missing/invalid pairs, and keep JMH test/profile scoped.
- `tools/phase24-requirement-matrix.tsv; tools/phase24-conformance-coverage.tsv; tools/phase24-release-audit.sh and related audit inventory contract tests`: Map every new requirement to exact executed tests/tool gates, update expected inventory IDs consistently and preserve audit target-tree/worktree restoration.
- `.internal-dev/changelogs/**; .internal-dev/reviews/**; .internal-dev/knowledge/{syntax-direct-calls-and-builtins,nominal-session-linkage,lexer-numeric-and-unicode-validation,performance-evidence-validation,public-api-validation}.md and other directly affected records`: Record each completed tranche, baseline HEAD, validation/specification impact, significant corrections and final review. Use exclusive creation and compliant templates without overwriting existing artifacts.

## Constraints

- The reported planning baseline is master at 43ccb06dcdd7218e0158d6595fc56fcc4cfeaf7f. Read-only inspection confirms the local master ref, not worktree cleanliness or current origin equality. Recheck status, HEAD and remote state before implementation and preserve all unrelated changes; never reset/overwrite them.
- Use Java 25 direct Class-File API production bytecode only. No interpreter, source replay, copied retained values, fake values, placeholders, schema-only constructors or second/generic Object execution ABI.
- The accepted source and command changes are immediate breaks without aliases or an edition selector. Preserve unprefixed primitive/String conversions and Array/Tuple construction; do not generalize :Type into new type APIs.
- Preserve exact declaration/storage links, nominal schema identity, factory/closure authentication, sourceLocal, imported mutation restrictions, owner confinement, staged publication, lifecycle/reset/close, safe points and fail-closed structured diagnostics. Performance is not authority.
- Named-call optimization removes only redundancy backed by authenticated typed entry/write boundaries and exact resolution proof. Keep dynamic authentication for unproven routes and preserve mutable self-rebinding semantics.
- Signature fields are producer/instance scoped metadata, not value authorization. Nominal signatures resolve only against the producer's exact schema environment; use before initialization or after invalid lifecycle transitions is forbidden.
- Route authority is occurrence-specific, survives legitimate saved-value propagation and field replacement, and does not alter raw closure identity or bless unrelated occurrences. New public helper surface must be strictly necessary to this capability boundary, not a speculative framework.
- Only the settled sibling-:: comma exception and parenthesized-direct preservation are added. Existing comma-capable lists keep their rules; no general grouping, arm-separator feature, statement punctuation or semicolon is introduced.
- Independent oracles/models, saved counterexamples, exhaustive inventories and default fuzz remain mandatory. Never delete assertions, suppress invariants, skip changed behavior or accept unsupported-emission errors for valid source.
- All changed syntax/runtime behavior, its consumers, fuzz/oracles and coverage documentation must land in the same completed tranche. Run focused suites and mvn test before each completion commit; run the documented extended fuzz campaign for language/runtime changes. Build-breaking substeps are not phase completions.
- Run final Java 25 clean verification, graphical editor qualification on a display, full Phase 23 and mandatory Phase 24. Unavailable environments are concrete validation blockers, not passing results; existing platform N/A policy remains distinct from unexpected skips.
- Follow the internal-dev record/commit workflow. Use internal_dev exclusive creation when available; otherwise use an available exclusive-creation mechanism, never overwrite a new artifact target. Record completed reviews and durable lessons. Log genuinely unrelated discovered defects and follow the repository's GitHub mirroring rule without absorbing them into this job. Do not push or publish releases; close in-scope GitHub issues only with passing committed evidence.

## Assumptions

- The supplied issue snapshot and recorded owner answers authorize #6–#13. Live GitHub completeness/status must be revalidated at execution; this review does not claim a fresh network query.
- Owner interview answers supersede conflicting historical issue suggestions and current specification text: especially broad exact named-call parity, per-instance rather than static/global signature hoisting, backslash commands and route-scoped saved values.
- Current completed nominal factories and the closed retained-transfer algebra are the baseline. #7 and #8 are specific member-boundary defects, not authority to reopen unrelated provenance work.
- Parameter/capture/mutable/import/session routes qualify for parity only with exact resolver evidence and an authenticated complete value-entry/write boundary; this condition must be verified rather than inferred from current F-expression behavior.
- Occurrence-scoped route evidence must integrate with existing exact typed storage/certificates without copying state or changing selected closure identity. Establish and adversarially validate its concrete representation before building the optimization on it. If evidence shows an unavoidable material public-contract conflict, stop the affected path and escalate rather than weaken authentication.
- Explicit nominal construction preserves semantic factory/descriptor behavior, not byte-identical classes or revisions: source and language-version changes legitimately alter identities, metadata and source maps.
- Execution has or can obtain GitHub authorization and the Java/Maven/JMH/display resources required for qualification. Missing access blocks the corresponding operation or validation, not unrelated implementation; no unavailable gate may be reported passed.
- Native macOS/Windows qualification beyond the repository's existing platform-neutral tests and documented platform N/A policy is not added.

## Settled Decisions

- Implement snapshot-open #6–#13 only; closed issues receive regression validation.
- Remove compiler and runtime LyraType primitive aliases in favor of PrimitiveType, explicitly recording the compatibility break.
- Adjacent bare negative numerals normalize to unary minus; trivia-separated signs do not.
- Keep parenthesized built-ins and only bare bracket built-ins; reject old direct-accessor special forms.
- Use marker-free subject match and parenthesized cond, with no conditional match, ?? or bracket cond.
- Use explicit nominal :Type[...] / :module->Type[...] only; preserve unprefixed built-in construction/conversions and ordinary indexing.
- Use backslash-only REPL developer commands; colon-leading input is source and classification/analysis remains nonexecuting.
- Use value access :. for S-expression method invocation and :: for direct bracket invocation, selecting the same current slot before arguments.
- Canonicalize every exact proven named storage route with sound boundary authentication; do not optimize opaque computed targets by shape.
- Use only the narrow sibling-:: comma rule, consistently across affected contexts, and the explicit (::direct[...]) preservation case.
- Hoist expected dynamic signatures into deterministic producer-scoped per-generated-instance fields and retain per-call live-value authentication.
- Use occurrence-specific retained field delegation with normal saved/captured/aggregate/later-submission value behavior; raw imported closures remain rejected even after delegated use.
- Require normalized structural parity and the full-protocol JMH average-time primary-score S/F ratio <=1.10; do not waive timing on structural success or cherry-pick statistics.
- Deliver source parsing, semantics/emission, all client surfaces and active fixture/model/docs migration as one end-to-end completed tranche, rather than committing an intentionally broken intermediate language.
- Version breaking language/runtime contracts coherently under existing metadata/compatibility rules without adding an edition, protocol version or alternate ABI.
- Post exact commits/tests and close an issue only after every mapped criterion and grouped validation passes.

## Implementation Approach

1. Preparation — read current task-relevant guides/specifications/knowledge; capture git status/HEAD, Java/Maven versions and fresh paginated GitHub list/body/comment evidence for #6–#13. Build the full acceptance-to-target/test/tranche matrix, recording closed #2–#5 regression references. Re-run bounded focused baseline reproductions for #6 performance/forms/tail/nilable-Fn, all four #7 cases, #8 nilable member consumers, #9–#12 syntax and #13 initialization orders. Distinguish actual results from historical claims; reconcile material drift before affected edits.
2. Tranche 1 — primitive foundation (#13): migrate all compiler/runtime/consumer/static-import/API-example aliases, remove interface fields and stale comments, and add isolated-loader tests for every primitive in both orders/modules. Document the Java API compatibility break and audit/update the relevant ABI compatibility declaration/tests as required by the runtime contract. Run focused type/nominal metadata/API suites and full mvn test, update bug/specification impact/knowledge/changelog, review the diff and commit. Close #13 only when its complete mapped acceptance passes.
3. Tranche 2, internal substep A — coordinated source contract (#6 parser findings, #9–#12): update living decisions/specifications/readable grammar with accepted new syntax, contextual comma exception and diagnostics. Implement cond, explicit construction, bare special forms, adjacent negative normalization, obsolete-form rejection and narrow direct-call parenthesization across tokens/descriptors/AST/replay/visitors. Write focused parser/span/composition tests, including marker-free match subject/pattern/result direct-call boundaries. This is not a phase completion or a standalone claim of a working language.
4. Tranche 2, internal substep B — complete semantics, flow, IR and JVM support: resolve exact nominal types versus uppercase value indexing and built-in type applications; type/lower dedicated source cond and marker-free match with contextual/nil/equality behavior, lazy effects and tail facts; preserve loops and unary-minus semantics. Update closed-IR/semantic/summary/transfer inventories and independent validation. Synchronize compiler/runtime language-contract identities and metadata constants, repair hard-coded ArtifactAssembly publication prefixes, and add current/stale-version packaging/loading tests. Prove factory descriptors, argument/default/body order and typed ABI semantics are preserved after normalizing intentional source/identity differences.
5. Tranche 2, internal substep C and completion — migrate plain/JLine/attach/editor command dispatch, completeness, help, completion, highlighting and source-only history to backslash; prove colon source compilation/execution versus nonexecuting type/analysis, including multiline and program-input separation. Migrate every affected active valid test, example, corpus fixture, generator, model, benchmark source and coverage/release document now, not in a later cleanup phase. Preserve original replay evidence and add migrated semantic twins plus removed-syntax negatives. Run all focused compiler/runtime/session/client/editor suites, full mvn test and extended fuzz; run available graphical qualification for changed editor behavior. Update records, review and commit only the complete end-to-end migration. Close #9–#12 only if their full mapped criteria, including required client validation, pass; #6 remains open.
6. Tranche 3 — retained nominal members (#8 then #7, one integrated completed boundary unit): derive exact member nilable contracts in independent semantic/IR sealing, covering AOT and multi-generation annotations/coalesce/predicate/value-match. Before implementing delegation, specify the concrete route occurrence carrier, issuance/validation, selected-value identity, private access, propagation, mutable replacement and lifecycle rules using existing compiler/session/runtime boundaries. Implement exact object authentication and narrow field-derived delegation without sourceLocal/general-authentication changes, threading evidence through getters/direct calls, nested leaves, saved/captured/parameter/return/aggregate values and later session publication. Independently revalidate producer certificates; do not trust caller-minted route data.
7. Tranche 3 qualification — flip all four #7 inventory cases together with actual values, callable invocation and exactly-once effects. Add anti-laundering raw-before/raw-after/coexisting-occurrence negatives, mutable field replacement with saved selection, wrong/private/forged routes, schemas/signatures/types/domains, initialization/failure/reset/close and multi-generation propagation cases. Keep SessionClosureAuthorityTest assertions and nominal factory/transfer/self-provenance lifecycle suites green. Update independent retained/session models and generators in this tranche, run focused suites, full mvn test and extended fuzz, update records, review and commit. Close #7/#8 only after complete acceptance.
8. Tranche 4 — call homogeneity and performance (#6), building on finalized route semantics: establish canonical post-resolution named-call classification and independent proof validation for local/recursive, parameter, capture, mutable, intrinsic, import/session and proven member routes. Audit all entry/write authentication before eliminating redundant checks; preserve selected target-before-argument order and full dynamic handling of computed targets. Extend self-tail lowering with mutable/aliased self safeguards and match/cond tail propagation. Cover target/authentication/argument/invocation failures in generated LYR-STACK regions without swallowing other VM errors or duplicating nested frames. Repair nilable-Fn class planning before stack-map/checkcast use. Add deterministic initialized-once producer-scoped expected-signature fields for dynamic calls.
9. Tranche 4 qualification — build the complete S/F matrix for results, side effects, selected mutable slots, receiver semantics, arity/type errors, mapped source ranges, tail depth, intrinsics and route/lifecycle/authentication rejection. Compare normalized generated call graphs/instructions, proving no redundant parse/cache/auth block on eligible paths and required checks on dynamic/entry paths. Add paired observable fib/direct-name JMH fixtures and machine-readable <=1.10 primary-score gate; extend evaluator self-tests/contract tests and JSON/CSV/text evidence for passing, failing, duplicate, missing, nonfinite and nonpositive denominator cases. Run focused tests, full mvn test, extended fuzz and full Phase 23 gate with all existing thresholds. Update #6 records/specs/docs/changelog, review and commit. Close #6 only when every mapped criterion passes.
10. Final qualification — preserve failing replay files/transcripts/seeds before any clean, then run Java 25 mvn test, mvn clean verify, the documented language-focused Maven selection, ./tools/fuzz-language.sh at documented extended budgets, and mvn -pl lyra-editor -am verify -Dlyra.editor.uiTests=true on a graphical display. Run fresh full tools/phase23-evidence.sh and tools/phase24-release-audit.sh, including exact executed matrix references and target-tree restoration. Diagnose and fix all in-scope failures; report proven unrelated/environment failures separately without marking an unmet criterion complete.
11. Final review/commit — inspect the complete diff and issue acceptance matrix for omitted behavior, stale active old spellings, primitive aliases, unversioned incompatibilities, unintended public API growth, global/closure-wide authorization, placeholders, unsupported valid IR, accidental history deletion and unrelated edits. Verify failure/source mapping, cancellation, lifecycle, reset, factory order and staged publication remain intact. Persist the required final review/changelog/knowledge updates with real baseline HEAD, validate any final changes and commit the completed unit.
12. GitHub reconciliation — for each #6–#13, post exact implementation commit(s), acceptance test methods/commands and gate evidence, closing only fully satisfied issues. Re-query all eight states; if final integration invalidates an earlier closeout, reopen with precise evidence. Archive/update local reports consistently and commit any resulting record changes. Report every still-open issue or unavailable required check with the exact unmet criterion; do not push or publish a release.

## Validation Criteria

- The execution evidence matrix accounts for every #6–#13 body/comment acceptance item and owner answer, with no scope drift; closed #2–#5 regressions remain green. Live status and cleanliness are measured, not assumed.
- Both LyraType interfaces have no primitive alias fields, all repository consumers use PrimitiveType, and isolated nondelegating type-loader tests verify all fourteen constants/type operations under both initialization orders in both modules. API compatibility impact and artifact ABI decisions are explicit and tested.
- Only accepted built-in bracket forms and their parenthesized equivalents execute. Obsolete :: special forms, qualified attempts and malformed reserved-keyword use produce stable structured diagnostics with correct UTF-16 spans; loop callback/effect/cancellation behavior is unchanged.
- Marker-free match and parenthesized cond pass subject-once/fallback-only, source-order/lazy selector/guard/result, truthiness, structural/numeric equality, contextual result/nilability, callable/provenance and tail tests. Old ??/match _, cond when/brackets, missing/nonfinal fallback, post-fallback arms and ordinary comma arms fail. The narrow sibling-:: boundary is tested separately from generic comma-arm rejection, including namespace arrows, nested forms and comments.
- Adjacent negative literals pass signed minima/maxima/neighbors, contextual/forced types, exponent/suffix/underflow/nonfinite, signed-zero bit assertions, arguments, aggregates, range bounds/steps, match/cond and constant/runtime overflow tests. Unsigned/malformed/nonadjacent forms fail appropriately; unary operator forms, binary minus and exponent signs retain their existing meanings and range permissions.
- Explicit :Type and qualified construction pass zero-argument/positional/class/nested/imported/selective/retained cases, private/type lookup failures, exact argument/default/body order, failures and source maps. Old unprefixed nominal applications, including old qualified construction, fail on resolved nominal use, not capitalization; uppercase indexing, conversions and Array/Tuple remain valid with equivalent normalized factory/descriptor behavior.
- Plain/JLine/attach/editor use backslash-only commands and nonexecuting \type/analysis. Colon-leading nominal construction is submitted as source and explicitly executes correctly; strings/comments, pending multiline source and program readLine input are not commandeered by prefix recognition. Help, completion, highlighting, history, EOF/unknown-command diagnostics and bounded source arguments agree across surfaces without a protocol change.
- Every eligible named-route S/F pair has equivalent results/effects, target and argument order, current mutable slot/receiver selection, intrinsic behavior, error codes and corresponding syntax-mapped spans/frames. Source locations need not be numerically equal between different spellings. Program/block/list/contextual sibling commas and (::f[...]) work; unseparated postfix remains receiver invocation; invalid separators remain rejected.
- Normalized proven-path bytecode has equivalent call/control-flow shapes without redundant per-call signature parse/cache/authentication blocks. Boundary/member storage checks and callee lifecycle/safe points remain. Dynamic targets and injected foreign/arbitrary-SAM/wrong-contract values remain fully authenticated and rejected before unauthorized execution.
- Dynamic signature metadata is deterministic and initialized once per generated instance/signature after authority/schema availability; nominal schemas resolve in the exact producer environment. Multiple instances/roots/revisions cannot share live authority accidentally. Initializing, failed and closed producer cases and per-call owner/thread/lifecycle/safe-point checks remain correct on field hits.
- S/F true self-tail recursion exceeds the existing established deep regression without stack growth, including match/cond result positions. Mutable rebinding, aliases and captured old closures cannot cause false self-tail looping. Non-tail target/argument/invocation overflow yields correctly ordered source-mapped LYR-STACK frames, while another VirtualMachineError escapes unchanged.
- Nilable-Fn coalesce compiles/verifies/executes all valid nullable/non-null branch cases and rejects semantically invalid cases with ordinary diagnostics, never LyraCompilerBugException. Nominal current-slot method S/F parity is proven against the current backend.
- The full Phase 23 protocol produces present, finite positive equivalent S/F average-time scores with score(S)/score(F) <=1.10, mandatory structural parity and all existing latency/allocation/footprint gates passing. Evidence includes fixture/source identity, environment, forks, warmup/measurement and uncertainty; no smoke or selected favorable statistic substitutes for a gate. Evaluator tests fail closed on missing/duplicate/invalid rows and show both sides of the threshold.
- Same- and cross-generation nilable nominal member annotation/coalesce/predicate/value-match cases consume independently derived exact contracts and execute; nilable receivers, type/contract mismatches, forged links and invented match narrowing stay rejected. No valid case reaches LYC-IR-003.
- All four #7 inventory cases succeed across declaration, construction and later observation, with exact Unit/output values and actual namespace-field callable invocation. Construction effects occur exactly once; reads do not replay them.
- Saved/captured/parameter/return/aggregate/later-submission field-derived values preserve exact selected identity and remain usable after a legal field replacement; new reads select the replacement. Raw imported occurrences remain rejected before and after delegated use and when colocated with routed occurrences. Wrong/private/forged routes, schemas, signatures, unrelated sessions/roots, retired epochs and failed/initializing/closed producers fail before unauthorized execution. sourceLocal and existing SessionClosureAuthorityTest assertions are unchanged.
- The closed retained-transfer algebra, independently certified fresh/member/self provenance, producer-bound factory ordering, exact storage identity, staged publication, cancellation, source mapping, reset/close and artifact/session lifecycle suites pass in AOT and real source-to-host-JVM/session integrations.
- Compiler/runtime language versions and revision/export derivation agree; current-version class/thin/bundled metadata and embedded facade publication are deterministic and load/invoke successfully. Stale/incompatible language/ABI artifacts fail with the specified compatibility diagnostics before initialization. Schema version is not confused with the source-language version; compatibility changes are documented.
- Active valid corpus/generator/oracle/examples use the new contract, removed forms have explicit negatives, and original counterexample evidence plus migrated semantic regression equivalents are retained. Default and extended fuzz cover all required changed families/profiles; expectations are independent of production helpers and failing seeds/transcripts are preserved before cleanup.
- Each completed tranche has focused validation, passing mvn test, applicable extended fuzz, complete consumer/model/docs updates, reviewed in-scope diff, compliant records and its own commit. Final mvn clean verify, focused language/session/runtime/CLI/editor suites, graphical verification, full Phase 23 and Phase 24 pass with no unexpected skips or audit restoration drift. Unavailable/unrelated checks are explicitly unfinished, not relabeled as success.
- Final commits contain only in-scope files and no placeholders, unauthorized compatibility/authority relaxations or unrelated edits. Every closed issue has exact commit/test evidence for all criteria; subsequent failures reopen it, and any still-open issue or remaining gate blocker is reported precisely.

## Out of Scope

- Closed #2–#5 implementation beyond preserving regression coverage, and issues newly opened after the snapshot unless scope is explicitly revised.
- Primitive/Array/Tuple :Type prefixes, type-level APIs, static members, inheritance, interfaces, user generics, destructuring/type patterns, bracket cond or other speculative language features.
- A global optimizer, whole-program devirtualization, speculative optimization of arbitrary computed callable targets, or removal of dynamic authentication.
- Changing sourceLocal, treating a session as one artifact, accepting arbitrary Java SAMs, globally blessing closure identities, or weakening exact producer/schema/private-access/lifecycle/thread authority.
- Source replay, runtime IR interpretation, copied retained values, schema-only nominal construction or a generic Object/second execution ABI.
- General parenthesized grouping, semicolon statements or general comma-separated forms/arms; only the accepted contextual sibling-:: exception is added.
- Runtime speedup or byte-identical artifact claims for explicit nominal syntax; prove equivalent construction lowering and no regression while allowing intentional version/source identity changes.
- An edition/legacy syntax mode, colon-command aliases, or protocol version/new remote operations solely for client command-prefix migration.
- Rewriting original saved counterexamples or historical records into fictitious new-syntax history; migrated semantic tests supplement preserved evidence.
- Native macOS/Windows installer certification, pushing commits, release publication and unrelated refactoring. Repository-required logging/mirroring of discovered unrelated defects does not add their implementation to this job.

## Planning Record

- Interactive rounds completed: 5
- User questions answered: 15
- Red-team validation: PASS
- Red-team validator: openai-codex/gpt-6-astra:high
- Red-team validation attempts: 1
- Inferable issues corrected: 19
- Validated at: 2026-09-14T00:53:14.298Z
- Remaining consequential open questions: none
